#Merge Strings Alternaltely
class Solution(object):
    def mergeAlternately(self, word1, word2):
        merged = ''
        for i in range(min(len(word1),len(word2))):
            merged += word1[i]+word2[i]
        merged+=word1[i+1:]+word2[i+1:]
        return merged
